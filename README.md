This is a Demo Project to build the frontend for a Form with multiple Steps.

Used Technologies: React 16.6+ & Material UI

To install the project, just run:

### npm install

To start the project locally, run

### npm start

To build the assets for the project, to deploy it on a server run

### npm run build
